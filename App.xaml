<Application x:Class="MZResourceManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MZResourceManager.ViewModels"
             xmlns:views="clr-namespace:MZResourceManager.Views"
             StartupUri="MainWindow.xaml">
        <Application.Resources>
                <ResourceDictionary>
                        <ResourceDictionary.MergedDictionaries>
                                <ResourceDictionary Source="Resources/AppColors.xaml"/>
                                <ResourceDictionary Source="Resources/ButtonStyles.xaml"/>
                                <ResourceDictionary Source="Resources/ScrollBarStyles.xaml"/>
                                <ResourceDictionary Source="Resources/CardStyles.xaml"/>
                        </ResourceDictionary.MergedDictionaries>
                        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

                        <DataTemplate DataType="{x:Type vm:UnusedResourcesViewModel}">
                                <views:UnusedResourcesView/>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type vm:ScriptBookExportViewModel}">
                                <views:ScriptBookExportView/>
                        </DataTemplate>

                        <Style TargetType="DataGrid">
                                <Setter Property="AutoGenerateColumns"
                                        Value="False"/>
                                <Setter Property="HorizontalScrollBarVisibility"
                                        Value="Auto"/>
                                <Setter Property="VerticalScrollBarVisibility"
                                        Value="Auto"/>
                                <Setter Property="CanUserAddRows"
                                        Value="False"/>
                                <Setter Property="CanUserDeleteRows"
                                        Value="False"/>
                                <Setter Property="CanUserResizeRows"
                                        Value="False"/>
                                <Setter Property="IsReadOnly"
                                        Value="True"/>
                                <Setter Property="SelectionMode"
                                        Value="Single"/>
                                <Setter Property="GridLinesVisibility"
                                        Value="Horizontal"/>
                                <Setter Property="BorderThickness"
                                        Value="0"/>
                                <EventSetter Event="PreviewMouseWheel"
                                             Handler="DataGrid_PreviewMouseWheel"/>
                        </Style>
                </ResourceDictionary>
        </Application.Resources>
</Application>
