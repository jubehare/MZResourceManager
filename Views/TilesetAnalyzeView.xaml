<UserControl x:Class="MZResourceManager.Views.TilesetAnalyzeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                BorderBrush="#D1D1D1"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#FFFFFF"
                        Padding="14,12"
                        BorderBrush="#D1D1D1"
                        BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Tilesets"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource AppColorSide2Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding FilteredTilesets.Count, StringFormat=' ({0})'}"
                                   FontSize="11"
                                   Foreground="{StaticResource AppColorMain2Brush}"
                                   VerticalAlignment="Center"
                                   Margin="4,0,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1"
                        Background="White"
                        Padding="8,8"
                        BorderBrush="#D1D1D1"
                        BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="&#xE721;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="13" Foreground="#999999"
                                   VerticalAlignment="Center"
                                   Margin="0,0,7,0"/>
                        <Grid Grid.Column="1">
                            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                                     Padding="6,4" FontSize="12"
                                     BorderBrush="#D1D1D1" BorderThickness="1"/>
                            <TextBlock Text="Search…"
                                       IsHitTestVisible="False"
                                       Foreground="#AAAAAA" FontSize="12"
                                       Padding="8,5" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding FilterText}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Grid>
                </Border>

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding FilteredTilesets}"
                         SelectedItem="{Binding SelectedTileset}"
                         Background="White"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Id, StringFormat=D4}"
                                           FontSize="10"
                                           VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground"
                                                    Value="#767676"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                             Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="White"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Name}"
                                           FontSize="12"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="White"
                    Padding="18,14"
                    BorderBrush="#D1D1D1"
                    BorderThickness="0,0,0,1"
                    MinHeight="60">
                <Grid>
                    <TextBlock Text="Select a tileset to inspect"
                               Foreground="#999999"
                               FontSize="13"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Visibility="{Binding ShowNoSelection, Converter={StaticResource BoolToVis}}"/>

                    <StackPanel VerticalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility"
                                        Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ShowNoSelection}"
                                                 Value="False">
                                        <Setter Property="Visibility"
                                                Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,0,0,2">
                            <Border Background="{StaticResource AppColorMain2Brush}"
                                    CornerRadius="4"
                                    Padding="8,3"
                                    Margin="0,0,10,0">
                                <TextBlock Text="{Binding SelectedTileset.Id, StringFormat=D4}"
                                           FontSize="11"
                                           FontWeight="Bold"
                                           Foreground="White"
                                           FontFamily="Consolas"/>
                            </Border>
                            <TextBlock Text="{Binding SelectedTileset.Name}"
                                       FontSize="15"
                                       FontWeight="SemiBold"
                                       Foreground="#1A1A1A"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding SelectedTileset.Note}"
                                   FontSize="11"
                                   Foreground="#3C3C3C">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility"
                                            Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedTileset.Note}"
                                                     Value="">
                                            <Setter Property="Visibility"
                                                    Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        BorderBrush="#D1D1D1"
                        BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0"
                                Background="#F3F3F3"
                                Padding="12,8"
                                BorderBrush="#D1D1D1"
                                BorderThickness="0,0,0,1">
                            <TextBlock Text="TILE SHEETS"
                                       FontSize="9"
                                       FontWeight="Bold"
                                       Foreground="#767676"/>
                        </Border>
                        <DataGrid Grid.Row="1"
                                  ItemsSource="{Binding Sheets}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Slot"
                                                    Binding="{Binding Slot}"
                                                    Width="50"/>
                                <DataGridTextColumn Header="Sheet"
                                                    Binding="{Binding SheetName}"
                                                    Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Maps using this tileset -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0"
                            Background="#F3F3F3"
                            Padding="12,8"
                            BorderBrush="#D1D1D1"
                            BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="MAPS USING THIS TILESET"
                                       FontSize="9"
                                       FontWeight="Bold"
                                       Foreground="#767676"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding MapsUsing.Count, StringFormat=' ({0})'}"
                                       FontSize="9"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource AppColorMain2Brush}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding MapsUsing}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Map"
                                                Binding="{Binding Display}"
                                                Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
